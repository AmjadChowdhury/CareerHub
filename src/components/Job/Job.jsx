

const Job = ({job}) => {
    const {id,logo,job_title,company_name,remote_or_onsite,location,job_type,salary} =job
    return (
        <div>
            <h1>Job</h1>
        </div>
    );
};

export default Job;